# Edge deploy встроен в deploy.yml (job deploy-edge).
# Этот файл — заглушка, чтобы не путать со старым отключённым workflow.

name: Deploy frontend to edge VPS (use deploy.yml)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Edge deploy moved
        run: |
          echo "Edge-VPS деплоится вместе с FirstVDS: workflow Deploy frontend to VPS (deploy.yml)."
          echo "Добавьте секреты EDGE_PROD_HOST и EDGE_SSH_PRIVATE_KEY в GitHub Actions."
          echo "См. deploy/DEPLOY_EDGE_VPS.md"
