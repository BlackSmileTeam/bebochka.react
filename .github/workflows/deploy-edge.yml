# Edge-VPS: отключено, пока нет нового сервера.
# Инструкция на будущее: deploy/DEPLOY_EDGE_VPS.md в монорепо bebochka.
# Активный деплой: deploy.yml → FirstVDS (PROD_HOST, PROD_SSH_PRIVATE_KEY).

name: Deploy frontend to edge VPS (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Edge deploy not configured
        run: |
          echo "Новый edge-VPS пока не арендован — этот workflow намеренно выключен."
          echo "Секреты EDGE_PROD_HOST и EDGE_SSH_PRIVATE_KEY добавлять не нужно."
          echo ""
          echo "Деплой на FirstVDS идёт через workflow: Deploy frontend to VPS (push в main)."
          echo "Когда появится edge-VPS — восстановите полный deploy-edge.yml из коммита ee5843f."
          exit 1
